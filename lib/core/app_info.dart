/// The version shown in Settings.
///
/// Kept in step with `version:` in pubspec.yaml by hand. Reading the real one at
/// runtime needs a plugin whose only job would be to tell you a number you
/// already typed once, and the release script checks the two match.
const appVersion = '1.2.0';

const appRepositoryUrl = 'https://github.com/Golde34/cairn';
